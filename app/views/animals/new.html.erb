<div class="container">
  <h1>Add your animal</h1>
  <%= simple_form_for([@park, @animal]) do |f| %>
    <%= f.input :name %>
    <%= f.input :image_url %>
    <%= f.submit 'Add', class: 'btn btn-primary' %>
  <% end %>
</div>
